services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: kume
      POSTGRES_USER: kume
      POSTGRES_PASSWORD: kume
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
